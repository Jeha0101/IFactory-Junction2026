# QA 수정 요구사항 (2026-08-23)

> 사용자가 직접 제품을 써보면서 찾은 문제들을 코드/실제 Firestore 데이터로 원인 확인한 뒤 정리한 문서입니다.
> **이 문서 기준으로 우선순위/방향을 확정한 뒤에 코드를 수정합니다** (개발 전 명세 확정 원칙).
> 각 항목: 증상 → 원인(코드 레벨로 확인됨) → 수정 방향 → 우선순위.

---

## 1. [내 정보]

### 1-1. 정보 수정 가능하게 (별도 화면 없이, 텍스트 필드+저장 버튼)
- **현재**: `web/src/app/profile/page.tsx`가 읽기 전용(`<dl>`)으로만 구현됨 — 이전에 "직접 입력 최소화" 원칙 때문에 편집 폼을 제거했었음.
- **수정 방향**: 같은 페이지에 각 필드를 `<input>`으로 바꾸고 하단에 저장 버튼 1개. 별도 편집 모드/페이지 없이 상시 편집 가능한 형태로. (에이전트 자동 채움 + 사용자 보정을 병행하는 구조로 복귀 — 처음 만들었던 폼과 비슷하지만 이번엔 "보정용"이라는 게 명확함)
- **우선순위**: 🔴 High

✅ **구현 완료 (2026-08-23)**: 텍스트 필드+저장 버튼 1개로 복귀. 실제로 저장→다른 페이지 이동→
복귀해도 값이 그대로 유지되는 것까지 확인함.

### 1-2. 값이 없는 필드 존재 (특히 성별)
- **원인 확인됨**: 실제 rawExtract를 까보니 `gender`가 아예 없음 — 에이전트 A가 빈 값이면 필드 자체를 안 주거나, 우리 쪽 `extractToProfilePatch()`가 빈 문자열을 걸러내서 (`nonEmpty()` 체크) profile에 반영을 안 함. 그런데 화면엔 **더미 시드값("남")이 그대로 남아있어서** 마치 실제 값이 있는 것처럼 보임 — 진짜 문제는 "값이 없다"가 아니라 "가짜 값이 진짜처럼 보인다".
- **수정 방향**: 1-1의 편집 필드로 가면 자연히 해결됨(빈 칸이면 빈 입력창으로 보임). 단, **더미 시드 데이터가 실제 데이터인 것처럼 섞여 보이는 문제를 반드시 같이 해결**해야 함 (아래 "공통 이슈" 참고).
- **우선순위**: 🔴 High (1-1과 묶어서)

✅ **구현 완료**: 목업 폴백은 "화면이 비어있지 않게" 유지하되(자세한 건 4-1 참고), 값이 없는
필드는 이제 빈 입력창(placeholder "없음")으로 정확히 표시됨.

### 1-3. 병역 정보는 저장하되 "내 정보" 화면엔서는 숨김
- **현재**: `PROFILE_FIELDS` 배열에 병역이 포함되어 화면에 노출됨.
- **수정 방향**: Firestore 저장(및 에이전트 매핑)은 그대로 유지하고, `profile/page.tsx`의 표시용 필드 목록에서만 병역을 제거.
- **우선순위**: 🟢 Low (간단)

✅ **구현 완료**: 표시 목록에서 병역 제거, 저장/매핑 로직은 그대로 유지.

### 1-4. 총학점 없으면 "없음"으로 표시
- **원인 확인됨**: `총학점` 필드는 애초에 어떤 에이전트 매핑도 연결되어 있지 않음(`agentMapping.ts`의 `PROFILE_FIELD_MAP`에 `총학점` 항목 자체가 없음) — 그런데 화면엔 더미 시드값 "4.5"가 그대로 남아 실제 값처럼 보임. (평균학점 쪽은 에이전트 A가 "0.0점 / 4.5점" 형태 문자열 하나로 뭉쳐서 주고 있어서, 평균/총학점을 애초에 분리해서 받고 있지 않음)
- **수정 방향**:
  - 값이 없으면 "없음" 표시 (일반 규칙으로 만들어서 다른 빈 필드에도 적용 고려)
  - 평균학점 문자열에서 총학점을 파싱해서 분리할지, 아니면 에이전트 A 스키마 자체를 평균학점/총학점 분리 필드로 요청할지 결정 필요 (팀원 상의 필요 — 우선은 파싱으로 임시 대응 추천)
  => 에이전트 A 스키마 자체를 평균학점/총학점 분리 필드로 요청으로 결정함
  
- **우선순위**: 🟡 Medium

✅ **"없음" 표시는 구현 완료** (모든 빈 필드에 placeholder="없음" 적용, 총학점 포함). 평균/총학점
분리는 팀원의 에이전트 A 스키마 수정 대기 중.

### 1-5. 페이지 들어갈 때마다 재조회하지 않기 (전역 캐시)
- **원인 확인됨**: `/profile`, `/documents`, `/resume-preview` 각각 독립적으로 `useEffect(() => { getProfile()... }, [])`를 갖고 있어서, 페이지를 이동할 때마다 Firestore를 다시 읽음. React 상태가 페이지(컴포넌트) 단위라 전역 캐시가 없음.
- **수정 방향** (아키텍처 변경, 신중히 설계 필요): 루트 레이아웃에 전역 데이터 프로바이더(React Context) 하나 두고, 앱 최초 로드 시 한 번만 구독 시작. 두 가지 방식 중 택1:
  1. **Firestore `onSnapshot` 실시간 구독** — 한 번 구독하면 이후 변경분만 자동으로 반영됨(로컬 캐시 내장), 재조회 로직을 직접 짤 필요 없음. 추천.
  2. 수동 캐시(모듈 전역 변수) + "저장/업로드 성공 시에만 명시적으로 refresh() 호출" — 구현은 간단하지만 실수로 stale 상태가 남을 여지 있음.
  >> 1번 방향으로 선택할 것
- **우선순위**: 🔴 High (체감 성능 문제 + 서류함 이슈 1번과 동일 원인)

✅ **구현 완료**: `AppDataProvider`(`web/src/lib/AppDataContext.tsx`)를 루트 레이아웃에 추가,
`onSnapshot`으로 profile/experiences/documents/coverLetterAnswers를 앱 최초 진입 시 한 번만
구독. 페이지 이동 후 돌아와도 재조회 없이 즉시 값이 보이는 것 확인함 (저장 → 서류함 이동 →
내정보 복귀 시 값 즉시 표시).

---

## 2. [서류함]

### 2-1. 파일 목록을 사이트 진입 시 미리 로드 (캐시)
- 1-5와 동일한 원인/해결책. 전역 데이터 프로바이더로 한 번에 해결.
- **우선순위**: 🔴 High

✅ **구현 완료** (1-5와 동일한 `AppDataProvider`로 해결). 문서 처리 상태(분석 중→완료)도
4초 폴링 대신 `onSnapshot` 실시간 반영으로 교체 — Firestore 읽기 횟수도 줄었음.

### 2-2. 여러 파일 동시 업로드
- **현재**: `<input type="file">`에 `multiple` 속성 없음, 1개씩만 선택 가능.
- **수정 방향**: `multiple` 추가 + `e.target.files` 배열 순회. **에이전트 처리(및 향후 다운로드)는 동시 실행 시 에러 유발 가능성이 있어 순차 처리**(for loop + await, `Promise.all` 금지)로 구현.
- **우선순위**: 🟡 Medium

✅ **구현 완료**: `multiple` 추가, 업로드+에이전트 처리 트리거를 for 루프로 순차 처리.

### 2-3. 자료 추가하기 박스에 드래그앤드롭 지원
- **난이도**: 낮음 (HTML5 `onDragOver`/`onDrop` 핸들러 추가, `preventDefault()`만 신경 쓰면 됨). 생략 안 해도 될 것 같습니다.
- **우선순위**: 🟢 Low

✅ **구현 완료**: 드래그해서 놓으면 업로드됨, 드래그 중 박스 색상도 바뀌도록 함.

### 2-4. (의견만) 파일에 마우스 올리면 세부정보 표시
- **제 의견**: 방향은 좋다고 생각해요(§4.5 출처 표시 원칙과도 맞음 — 분류/취득일/추출된 핵심값을 바로 보여주면 신뢰도가 올라감). 다만 **hover 방식은 모바일에서 아예 동작하지 않고**(터치 디바이스엔 hover 개념이 없음), 툴팁 위치 계산도 은근히 손이 갑니다. **클릭하면 펼쳐지는 상세 행(accordion) 방식**을 추천해요 — 데스크톱/모바일 둘 다 되고 구현도 hover보다 단순합니다. 지금 당장 급한 기능은 아니라 시간 남으면 하는 걸로 보류해도 좋을 것 같아요.
=> 좋음. 상세 행으로 가되 우선순위에 따라 나중에 구현하는 것으로 결정

---

## 3. [이력서 초안] — 가장 심각한 문제들

### 3-1. 성명란에 대학교, 생년월일에 대학교가 들어감 ⚠️ 치명적
- **원인 확인됨 (실제 데이터로 검증)**: Firestore의 `rawExtract.major` 값이 `"한국공학대학교 미디어디자인공학전공"`으로, **에이전트 A가 학교명과 전공을 분리하지 않고 하나의 `major` 필드에 합쳐서 줌**. 여기에 더해 **에이전트 B의 `cellRef`가 애초에 신뢰도가 낮다는 게 이미 확인된 상태**(이전 대화에서 라벨 칸 vs 값 칸 혼동 발견)라, `major`의 cellRef가 엉뚱하게 성명/생년월일 행을 가리켜서 그 값이 그대로 써진 것으로 보입니다.
  - 이건 단순 "행 안에서 라벨/값 칸 혼동"보다 심각한 **"아예 다른 행을 가리키는" 문제**라, 지금 `fill_values()`에 넣어둔 방어 로직(같은 행의 마지막 칸으로 보정)으로는 못 잡습니다 — 애초에 행 번호 자체가 틀렸기 때문.
- **수정 방향 (택1, 팀원과 상의 필요)**:
  1. **에이전트 A 스키마 수정**: `major`를 학교명(`school`/`university`)과 전공(`major`)으로 분리해서 받기 (근본 원인 일부 해결)
  2. **매칭 단계에 교차검증 추가**: 우리가 이미 갖고 있는 `python/docx_tools.py`의 `extract_structure()` 결과(라벨 텍스트를 100% 정확히 앎)와, 에이전트 B가 준 `cellRef`+`canonicalKey`를 대조해서 — 그 위치의 실제 원본 라벨이 canonicalKey와 명백히 안 맞으면(예: 라벨이 "성명"인데 canonicalKey가 "major") **그 매칭을 버림**. 근본적으로 더 안전하지만 구현량이 있음.
  - 시간 없으면 최소한 2안의 "명백히 안 맞으면 버리기"만이라도 넣는 걸 추천합니다 — 지금처럼 아예 다른 사람 정보로 오염되는 것보단 "못 채운 빈칸"이 훨씬 나음.
- **우선순위**: 🔴🔴 Critical
  => 1안과 2안 모두 채택할 것.

✅ **2안 구현 완료 (2026-08-23)**: `/api/analyze-form`에서 우리 python `extract_structure()`로
독립적으로 뽑은 실제 라벨과 에이전트 B의 canonicalKey를 대조, 안 맞으면 값을 버림
(`agentMapping.ts`의 `isPlausibleMatch`/`CANONICAL_KEY_KEYWORDS`). 실제 테스트로 검증:
- 첫 시도: "major" 값이 성명/생년월일 행을 가리킨 사고를 정확히 걸러냄 (7/24 → 안전하게 거부)
- 추가로 발견: 안내문 텍스트에 우연히 "성명"이라는 단어가 포함돼 있어 검증을 통과해버리는
  케이스 발견 → 라벨 후보를 25자 이하 짧은 텍스트로만 제한하도록 보강
- **새로 확인된 사실**: 에이전트 B가 **table 번호 자체를 통째로 잘못 지정**하는 경우도 있음
  (인적사항 전체가 table 1이 아니라 table 0으로 지정된 사례 실제 확인) — 단순 "행 안에서 라벨/값
  혼동"보다 훨씬 근본적인 문제. **정확한 좌표를 직접 넣으면 100% 정확히 써진다는 것도 재검증
  완료** — 문제는 전적으로 에이전트 B의 좌표 정확도. 1안(에이전트 A 스키마 분리)과 팀원에게 보낸
  cellRef 프롬프트 개선 요청이 여전히 유효하고 중요함.

### 3-2. "(예시)" 파란 글씨를 실제 이력으로 착각해서 추가함
- **원인 확인됨**: 실제 Firestore에 `기관: "(예시) AI 챗봇 개발"`, `기관: "(예시) Back-end"`가 실제 경력처럼 저장되어 있음 — 에이전트 A가 이 텍스트가 "예시/안내"라는 걸 구분 못함. (참고: 이건 저희가 테스트에 원본 빈 양식을 그대로 에이전트 A에 넣어서 생긴 특수 케이스이기도 하지만, 실사용자가 예시 행을 안 지우고 제출하는 경우도 충분히 있을 수 있어 방어가 필요함)
- **수정 방향**: 에이전트 A 프롬프트에 "값이 '(예시)'로 시작하거나 안내/placeholder처럼 보이면 실제 데이터로 추출하지 말 것" 명시 요청 (팀원 전달 필요). 문서 원본에서 이 텍스트가 파란색(안내색)이라는 서식 정보를 Upstage Parse가 보존 안 해줄 가능성이 높아서(HTML 출력에 색상 정보 없음, 확인됨), 텍스트 패턴 기반 필터링이 현실적인 방법.
=> 해당 이력서 유형을 데모에서 사용하지 않는 방법으로. 왜냐하면 그렇게 세부사항까지 수정을 하고 있기에는 해커톤 규모에서 부적절함.
- **우선순위**: 🟡 Medium (팀원 프롬프트 수정 필요, 우리 쪽에서 직접 못 고침)

### 3-3. 자기소개서 문항이 가짜 데이터 (하드코딩)
- **원인 확인됨**: `web/src/lib/mockData.ts`의 `MOCK_ESSAY_QUESTIONS`("협력 경험에 대해서...", "지원 동기를...")가 `resume-preview/page.tsx`에 **항상 고정으로** 렌더링됨 — 실제 업로드한 양식에서 에이전트 B가 감지한 진짜 문항("지원동기", "직무 관련 경험", "문제 해결 및 도전 경험")을 전혀 안 씀. `/api/fill-form`이 에이전트 B의 필드 목록을 이미 받아오고 있는데, 그중 자소서형 필드를 뽑아서 화면에 연결하는 코드가 아예 없었습니다 (제가 만들다 만 부분).
- **수정 방향**: `/api/fill-form` 응답에서 `canonicalKey`가 자소서형인 항목들을 추출해 실제 `questionText` 목록을 프론트로 전달 → `MOCK_ESSAY_QUESTIONS` 대신 이 실제 목록으로 `EssayQuestionBlock` 렌더링. (에이전트 B가 자소서형 필드를 어떻게 표시하는지는 최근 명세에 추가한 규칙 — 팀원이 반영했는지 확인 필요)
- **우선순위**: 🔴 High (저희 쪽 순수 구현 공백, 바로 고칠 수 있음)

✅ **구현 완료 (2026-08-23)**: `/api/analyze-form`이 `canonicalKey`가 `자소서QnA`인 필드를
실제로 뽑아서 돌려주고, `resume-preview`가 그걸로 `EssayQuestionBlock`을 렌더링하도록 교체함.
`MOCK_ESSAY_QUESTIONS` 완전히 삭제. **단, 실제 테스트에선 에이전트 B가 아직 어떤 필드도
`자소서QnA`로 태깅 안 하고 있어서(팀원 반영 대기 중) 화면엔 자소서 섹션 자체가 안 뜸** — 코드는
준비됐고 팀원 쪽 반영만 기다리는 상태.

### 3-4. "비슷한 질문을 찾았어요"도 가짜 데이터
- **원인 확인됨**: Firestore `coverLetterAnswers` 컬렉션에 실제 데이터가 **0건**이고, 8건 전부 `🧪 더미 데이터로 바로 초안 보기` 버튼(`MOCK_COVER_LETTER_ANSWERS`)에서 온 것. **자소서 Q&A를 실제 업로드 문서에서 추출해서 저장하는 파이프라인이 아예 구현 안 됨** — `/api/documents/[id]/process`가 프로필/경력만 처리하고 자소서 Q&A는 손도 안 댐. 게다가 에이전트 A의 실제 출력을 보면 자소서 내용이 `self_introduction`이라는 **문자열 하나**에 뭉쳐서 옴 (질문별로 안 나뉨) — 애초에 에이전트 A가 리스트 형태 Q&A 추출을 아직 구현 안 한 것으로 보임.
- **수정 방향**:
  1. 팀원에게: 에이전트 A가 `self_introduction`을 하나로 뭉치지 말고, 명세(§1 필드 사전의 `자소서QnA: [{질문,답변}]`)대로 **질문별 리스트**로 뽑아달라고 요청
  2. 우리 쪽: 그 리스트를 받으면 `/api/documents/[id]/process`에서 `addCoverLetterAnswer()`로 실제 저장하는 코드 추가 (지금 완전히 빠져있음)
- **우선순위**: 🔴 High (기능2의 핵심인데 사실상 미구현 상태였음 — 이번에 확실히 확인됨)

### 3-5. 경력/경험에 중복·미가공 텍스트
- **원인 확인됨 (둘 다 코드로 확정)**:
  1. **중복**: 같은 문서를 여러 번 처리(재처리/재테스트)할 때마다 `addExperience()`가 무조건 새로 추가되기만 하고 **기존 것을 지우거나 중복을 확인하는 로직이 없음**. 실제로 같은 `sourceDocumentId`에 완전히 동일한 경력이 2~3개씩 중복 저장된 게 확인됨.
  2. **미가공 텍스트 노출**: `agentMapping.ts`의 `recordToExperience()`가 알려진 필드명 후보(`description`, `skill_items` 등)로 못 찾으면 **원본 객체를 통째로 `JSON.stringify()`해서 설명란에 넣도록** 되어 있는데, 실제 "award" 레코드는 `award_detail`/`award_organization`/`award_period`라는, 제가 후보 목록에 안 넣어둔 필드명을 씀 — 그래서 `{"award_period":"", "award_detail":"...", ...}` 같은 원본 JSON이 그대로 노출됨.
- **수정 방향**:
  1. 문서 재처리 시 해당 `sourceDocumentId`의 기존 experiences를 먼저 삭제하고 새로 넣기(또는 내용 비교 후 중복 스킵)
  2. `recordToExperience()`의 후보 키 목록에 `award_organization`(기관), `award_detail`(설명), `award_period`(기간) 등 실제 관측된 키 추가. 이런 매핑 누락이 앞으로 또 생길 수 있으니, 후보에 없는 키가 나오면 로그를 남기거나 우리가 주기적으로 확인하는 절차도 필요.
- **우선순위**: 🔴 High (중복은 데이터 신뢰도 문제, 원본 JSON 노출은 사용자 경험 치명타)

✅ **구현 완료 (2026-08-23)**: 재처리 시 `deleteExperiencesBySource()`로 기존 것 먼저 삭제 후
새로 저장(중복 방지). `recordToExperience()`도 개선 — 후보 키를 못 찾으면 JSON 문법 그대로
노출하는 대신, 남은 값들만 사람이 읽을 수 있게 이어붙이도록 변경(향후 새 필드명이 또 나와도
원본 JSON이 새지 않음).

### 3-6. 경력/경험이 문서 빈칸에 안 들어가고 하단 목록에만 나옴
- **원인 확인됨**: 매칭 로직(`/api/fill-form`)이 **스칼라 값(문자열 1개)만 매칭**하도록 되어 있고, "여러 개를 반복되는 표 행에 나눠 넣기"(예: 프로젝트 경험 표에 경력을 하나씩) 로직이 아직 없음(§4.7 매칭단계의 2단계는 원래도 "미구현"으로 명시해뒀던 부분). 지금 하단 "경력/경험" 목록은 애초에 기능7(포함 여부 확인용)로 만든 별도 UI라, 문서 본문 채우기와 연결이 안 되어 있었음.
- **수정 방향**: `isRepeatable: true`인 필드에 대해 `experiences`에서 `type`이 맞는 항목들을 순서대로 골라 표의 반복 행에 나눠 넣는 로직 필요. **표에 원래 있는 행 수보다 경력이 많으면 python-docx로 행을 복제해서 추가해야 해서 난이도가 있음** — 별도 설계/구현 작업으로 분리 추천.
- **우선순위**: 🟡 Medium (중요하지만 3-1, 3-5보다 구현 난이도 높음 — 후순위 제안)

### 3-7. 다운로드한 최종 docx가 원본과 너무 달라짐 ⚠️⚠️ 가장 치명적
- **원인**: 지금 다운로드는 "실시간 편집 미리보기(docx-preview로 렌더링한 HTML)를 편집한 뒤, 그 HTML을 LibreOffice로 다시 docx로 변환"하는 방식입니다. 이 경로는 **변환이 두 번 겹칩니다**: ① 원본 docx → HTML(docx-preview, 근사치 렌더링) ② 그 HTML → docx(LibreOffice, 또 근사치 변환). 두 번의 손실 변환이 겹쳐서 서식이 크게 틀어집니다. 이건 애초에 "임시방편"이라고 말씀드렸던 부분인데, 실사용 테스트로 그 한계가 확실히 드러난 것입니다.
- **다행히 진짜 해결책은 이미 만들어져 있고 검증까지 끝났습니다**: `python/docx_tools.py`의 `fill_values()` — **원본 docx 파일을 절대 HTML로 바꾸지 않고, 정확한 셀 좌표에 텍스트만 갈아끼우는 방식**이라 서식이 100% 보존됩니다 (이전에 실제로 스크린샷까지 찍어서 검증함). `/api/fill-form`이 이미 이 방식으로 "최종 문서 미리보기"를 만들고 있어요.
- **수정 방향 (아키텍처 전환 필요)**:
  - 지금 구조: 미리보기 = `fill_values()` 결과(정확) → 근데 그걸 **다시 HTML로 열어서 편집 가능하게 만들고**, 편집 후 다운로드는 **그 HTML을 또 변환**(부정확) — 애써 정확하게 만든 걸 편집 단계에서 다시 부정확하게 만들고 있는 구조.
  - **제안**: "미리보기에서 자유롭게 아무 데나 편집"을 포기하고, 대신 **감지된 필드 목록을 편집 가능한 폼(라벨+입력창)으로 보여주고**, 그 값들을 `fill_values()`에 그대로 넣어서 미리보기와 다운로드를 **항상 같은 방식(fill_values)으로 생성**합니다. 사용자가 자동 채움 값이 틀렸으면 그 폼에서 고치면 되고, 미리보기(읽기 전용 시각적 확인용)와 다운로드가 완전히 동일한 파이프라인을 타서 "미리보기랑 다운로드 파일이 다르다"는 문제 자체가 안 생깁니다.
  - 이건 이력서 초안 페이지의 핵심 구조를 바꾸는 작업이라 **다음 개발에서 제일 먼저 다뤄야 할 항목**이라고 생각합니다.
- **우선순위**: 🔴🔴🔴 Critical — 다른 모든 것보다 먼저

✅ **구현 완료 (2026-08-23)**: 제안대로 아키텍처 전환함.
- `/api/analyze-form` (에이전트 B 실행 + 매칭, 문서 생성 안 함) / `/api/render-docx` (좌표+값으로
  `fill_values()` 실행, 문서만 생성) 두 라우트로 분리
- 미리보기(`DocxLivePreview`)는 이제 **읽기 전용**, contentEditable/HTML 편집 완전히 제거
- "감지된 필드" 편집 폼(라벨+입력창)에서 값 수정 → "미리보기 새로고침" 또는 입력창 blur 시
  `/api/render-docx` 재호출 → 미리보기 갱신. **다운로드 버튼도 정확히 같은 라우트를 호출** —
  구조적으로 "미리보기랑 다운로드가 다르다"는 문제가 재발할 수 없음
- 저장(`resumeDrafts`)도 HTML 스냅샷 대신 **값 맵(fieldValues)**을 저장하도록 변경 — 더 가볍고
  정확함
- 정확한 좌표로 직접 테스트해서 서식 100% 보존 재확인함 (성명/생년월일 값이 원본 서식 그대로
  정확한 셀에 들어감)
- `/api/fill-form`, `/api/export-docx`(구 HTML 변환 라우트)는 삭제함

---

## 4. 공통으로 발견된 문제 (여러 항목의 근본 원인)

### 4-1. 목업(mock) 데이터가 실제 데이터와 섞여서 진짜처럼 보임
지금 화면에 보이는 값 중 상당수(성별 "남", 총학점 "4.5", 이름 "홍길동" 등)가 실제 추출값이 아니라 예전 테스트용 `🧪 더미 데이터` 버튼이 남긴 값입니다. `saveProfile()`이 `merge:true`라서 실제 값과 목업 값이 필드 단위로 뒤섞여 있어요. **데모 전에 반드시 Firestore 데이터를 깨끗이 초기화**하고, 가능하면 목업 시드 버튼과 실제 파이프라인이 같은 프로필 문서를 공유하지 않도록(혹은 초기화 버튼을 만들어서) 정리하는 걸 추천합니다.

✅ **1차 초기화 완료 (2026-08-23)**: experiences/documents/coverLetterAnswers/resumeDrafts 전부 비우고 profile/rawExtract 리셋함. 다만 테스트를 계속하면 또 섞일 수 있으니, 데모 직전에 한 번 더 깨끗이 밀고 시작하는 걸 추천합니다.

### 4-2. 문서를 서류함에서 삭제해도 파생 데이터(경력 등)가 안 지워짐
`deleteDocumentRecord()`가 문서 자체만 지우고, 그 문서에서 나온 `experiences`/`profile` 기여분은 그대로 남습니다. 지금 당장 급한 건 아니지만, 정리 기능을 만들 때 같이 고려하면 좋을 것 같습니다.

### 4-3. 에이전트가 JSON 대신 평범한 문장으로 응답하면 그대로 크래시
**증상 (2026-08-23, 실사용 중 발견)**: 서류함 업로드 후 내 정보가 갱신 안 되고 "분류 실패"가 뜸,
이력서 작성 시 `Error: Unexpected token '정', "정보가 부족합니다."... is not valid JSON` 발생.

**원인**: 에이전트가 가끔(또는 특정 상황에서 항상) JSON 배열/객체 대신 "정보가 부족합니다." 같은
평범한 문장으로 응답하는데, `JSON.parse()`가 이걸 그대로 파싱하려다 알아보기 힘든 문법 에러를
던지고 있었음. 서류함(에이전트 A)과 이력서 작성(에이전트 B)이 같은 `runAgentOnFile()` 유틸을
공유해서 증상이 두 군데서 동시에 나타난 것.

✅ **구현 완료**:
- 에러 메시지를 `AgentNonJsonResponseError`로 명확하게 바꿈 (에이전트의 실제 응답 앞부분을 그대로 보여줌)
- 서류함: 문서별로 `errorMessage` 저장 + **"다시 시도" 버튼** 추가 (일시적 실패면 재시도로 해결될 수 있음)
- 이력서 작성: 에러 배너 옆에 **"다시 시도" 버튼** 추가

**추가로 찾은 진짜 원인 (에이전트 B)**: 에러 메시지 원문을 보니 "python-docx 전처리 구조 JSON이
포함되어 있지 않다"고 명시하고 있었음 — 팀원이 이전 피드백대로 프롬프트를 "구조 JSON을 요구"하게
고쳤는데, **저희 쪽 코드가 그 구조 JSON을 실제로 같이 안 보내고 있었음** (파일만 보냄). API 호출에
`input_text`로 구조 JSON을 같이 보내도록 수정 완료 (`upstage.ts`의 `runAgentOnFile`에
`additionalText` 파라미터 추가).

⚠️ **그런데 이것만으론 완전히 해결 안 됨**: 구조 JSON을 같이 보내니 이번엔 최종 JSON 배열 대신
"## 표 구조 기준 요약..." 같은 **설명글**로 응답함 (2번 연속 재현됨, 우연 아님). 이건 이전에 팀원께
전달드렸던 그 이슈("결과는 설명 없이 오직 지정된 JSON 배열 형식으로만 응답해라"를 프롬프트에 추가
요청)가 아직 반영이 안 된 것으로 보임 — **에이전트 B는 현재 이 프롬프트 수정 전까지는 실사용이
어려운 상태**입니다.

에이전트 A는 저희 테스트 파일로는 정상 동작 확인함 — 사용자님이 올리신 실제 파일에서만 발생했다면
파일 자체 이슈이거나 일시적 문제일 수 있어 재시도 버튼으로 먼저 확인 필요.

### 4-4. [정정] 4-3의 "에이전트 B가 설명글로 응답한다"는 진단은 틀렸음 — 우리 쪽 버그였음
같은 파일, 텍스트 유무만 다르게 A/B 테스트한 결과: **파일만 보내면 에이전트 B가 정상 JSON을
반환함**. 문제는 제가 추가로 주입한 안내 문구("다음은 python-docx로 미리 분석한... 사용하세요")가
모델을 "이 데이터를 설명해줘"로 착각하게 만든 것 — 팀원의 프롬프트는 처음부터 문제없었음.
`analyze-form`에서 이 문구 주입을 제거함(원복).

이 과정에서 팀원이 이미 반영해두신 것도 확인됨:
- ✅ `university`(학교) / `major`(전공) 분리 (3-1의 1안)
- ✅ `my_gpa`(평균학점) / `avg_gpa`(총학점) 분리 (1-4)
- ✅ `self_introduce: [{question, answer}]` 리스트 형태 자소서 Q&A 추출 (3-4)

세 가지 다 우리 쪽 매핑 코드(`agentMapping.ts`)에 반영 완료:
- `PROFILE_FIELD_MAP`에 university/my_gpa/avg_gpa 추가
- `extractToCoverLetterAnswers()` 신규 추가 → `/api/documents/[id]/process`에서
  `addCoverLetterAnswer()`로 실제 저장하도록 연결 (재처리 시 중복 방지 포함) — **3-4 완료**
- 빈 양식 테스트라 답변이 빈 문자열이라 저장 0건이었는데, 빈 답변은 애초에 저장 안 하도록 필터링
  되어 있어서 의도대로 동작한 것임 (실제 채워진 자소서로 테스트해야 진짜 검증됨)

**부수적으로 발견한 작은 버그**: my_gpa/avg_gpa처럼 서로 다른 canonicalKey가 같은 셀(cellRef)을
가리키는 경우 필드 목록에서 React key 충돌 경고 발생 — key만 고유하게 고쳐서 크래시는 막았으나,
두 필드가 같은 칸을 공유하는 근본 문제(값이 겹쳐 쓰일 수 있음)는 아직 안 고쳤음. 발생 빈도 낮아
후순위로 보류.

---

### 4-5. 서류함 "분류 실패"는 버그가 아니라 정상 분류였음 — 우리 쪽에서 처리 안 하고 있었음

2026-08-23, 재학증명서/봉사활동 지원서/수료증 3건을 업로드하니 전부 "분류 실패"로 떴던 문제.
4-3/4-4와 같은 패턴(에이전트가 JSON이 아닌 응답을 주면 무조건 에러 처리)인지 확인 요청받음.

**확인 결과 (실제 문서 ID로 라이브 재실행해서 검증함, 추측 아님):**
에이전트 A는 이력서일 때만 Extract용 JSON을 반환하고, 이력서가 아닌 문서는 분류 라벨
문자열 하나만 반환하는 것으로 확인됨 — 예: `"재학증명서"`, `"기타"`, `"수료증"`.
이건 에이전트 A가 잘못 응답한 게 아니라 **의도된 Classify 동작**이었고, 우리 쪽
`/api/documents/[id]/process`가 "Extract JSON이 아니면 전부 에러"로만 처리하고 있어서
정상 분류를 실패로 잘못 표시하고 있었음. (4-3의 "정보가 부족합니다" 같은 문장형 응답과는
다른 패턴 — 문장이 아니라 짧은 라벨 그 자체라는 점에서 구분했고, 실제 재실행으로 확인함)

**수정**: 에이전트 응답이 JSON이 아니면서 20자 이하 + 문장부호 없는 짧은 문자열이면
"분류 라벨"로 간주해 `status: "done"`, `category: <라벨>`로 저장하도록
`/api/documents/[id]/process`에 분기 추가. `DocumentCategory` 타입도 고정된 목록이 아니라
임의 문자열을 받도록 완화(`types/index.ts`).

기존에 실패로 떴던 3건을 실제로 재처리해서 확인함:
- 재학증명서(국문).pdf → `status: done`, `category: "재학증명서"`
- 봉사자 지원서 및 개인정보 활용 동의서.pdf → `status: done`, `category: "기타"`
- 수료증.pdf → `status: done`, `category: "수료증"`

**남은 과제**: 이력서가 아닌 문서 종류(재학증명서/수료증 등)는 아직 acquiredAt/expiresAt을
채워주는 Extract 스키마가 없음 — 기능3(자격증 만료 알림)을 실제로 쓰려면 팀원에게 문서
종류별 Extract 스키마 추가를 요청해야 함.

---

## 5. 제안 우선순위 (다음 개발 순서)

✅ = 완료, 나머지는 남은 작업.

1. ✅ **3-7 (다운로드 아키텍처 전환)**
2. ✅ **3-1 (엉뚱한 값 매칭 방지)**
3. ✅ **4-1 (데이터 초기화)**
4. ✅ **1-1/1-2/1-3/1-4/1-5, 2-1/2-2/2-3 (내정보 편집 + 전역 캐시 + 서류함 개선)**
5. ✅ **3-3, 3-5 (자소서 문항 실데이터 연결, 경력 중복/JSON 누출 수정)**
6. ✅ **3-4 (자소서 Q&A 실제 저장)** — 팀원이 이미 `self_introduce` 필드를 추가해줬고(§4-4),
   우리 쪽 저장 로직(`extractToCoverLetterAnswers` → `addCoverLetterAnswer`)도 연결 완료.
   **단, 실제 채워진 자소서 파일로는 아직 검증 안 됨** — 지금까지 테스트한 건 빈 양식뿐이라
   저장 0건이 정상 동작이었음(빈 답변 필터링). 진짜 답변 있는 자소서로 한 번 더 확인 필요.
7. **3-2 (예시 텍스트 필터링), 3-6 (반복 필드 문서 삽입)** — 팀원 프롬프트 수정 또는 구현 난이도 높음, 시간 되면
8. **2-4 (파일 상세정보 hover/click)** — 의견 제시만 완료, 필요시 나중에

**남은 것 중 지금 당장 우리가 더 할 수 있는 건 3-4를 실제 자소서 파일로 검증하는 것뿐이고, 나머지는 팀원 쪽 에이전트 개선 대기 또는 후순위 편의 기능입니다.**
