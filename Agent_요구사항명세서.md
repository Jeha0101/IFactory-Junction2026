# IFactory 에이전트 요구사항 명세서 (Agent A / Agent B)

> 이 문서는 `IFactory개발명세초안.md`의 §4.3·§4.7을 바탕으로, **에이전트 두 개를 만드는 팀원에게 전달할 상세 요구사항**만 뽑아 정리한 문서입니다. 전체 프로젝트 맥락이 필요하면 마스터 명세를 참고하세요.

---

## 0. 큰 그림

```
[업로드된 "이미 작성된" 문서]              [업로드된 "빈" 양식]
  이력서 / 자소서 / 증명서 등                채워야 할 지원서(DOCX)
        │                                        │
        ▼                                        ▼
   ┌─────────┐                          ┌─────────────────┐
   │ 에이전트 A │                          │ python-docx 전처리 │ ← 코드, AI 아님
   │(문서 이해) │                          │ (구조 감지, 이미 검증됨) │
   └─────────┘                          └─────────────────┘
        │ {canonicalKey: value, source}                │ (라벨, 힌트, 위치) 목록
        ▼                                        ▼
   Firestore 저장                          ┌─────────┐
   (개인 DB 아카이브)                        │ 에이전트 B │
                                          │(양식 이해) │
                                          └─────────┘
                                                │ {canonicalKey, blank위치, format, repeatable}
        └───────────────┬────────────────────────┘
                         ▼
                  매칭/리컨실 로직 (A·B 완성 후, 별도 통합 단계)
                         │
                         ▼
                  python-docx로 실제 파일에 값 기입
```

**A와 B는 서로 다른 입력(작성된 문서 vs 빈 양식)을 각자 독립적으로 처리**하고, 결과를 같은 "필드 사전"으로 표현합니다. 매칭은 A·B가 둘 다 완성된 후 이어지는 별도 단계입니다(§4 참고).

---

## 1. 공용 필드 사전 (계약) — **A, B 둘 다 반드시 이 키를 써야 함**

이게 제일 중요합니다. A와 B가 서로 다른 이름을 쓰면(예: A는 "이름", B는 "성명") 나중에 매칭이 텍스트 유사도 비교가 되어버려서 느리고 부정확해집니다. **같은 키를 쓰면 매칭은 딕셔너리 조회 한 줄로 끝납니다.**

| canonicalKey | 설명 | 실제 문서에서 본 표현 예시 | 타입 | 반복(list)? |
|---|---|---|---|---|
| `이름` | 성명 | "성명", "이름" | string | X |
| `생년월일` | 생년월일(+나이) | "생년월일(나이)" → 힌트: `0000년 00월 00일 ( 만 00세 )` | string | X |
| `성별` | 성별 | "성별" | string | X |
| `주소` | 주소 | "주소" | string | X |
| `연락처` | 전화번호 | "연락처" | string | X |
| `이메일` | 이메일 | "E-mail" | string | X |
| `학교` | 대학명 | "대학명" | string | X |
| `전공` | 주전공 | "학과명"+"주전공" | string | X |
| `복수전공` | 복수/부전공 (없으면 null) | "학과명"+"복수/부전공" | string, nullable | X |
| `학년` | 학년 | "학년('26.9월 기준)" | string | X |
| `평균학점` | 평균학점 | "전체평균학점(전학년)" → 힌트: `0.0점 / 4.5점` | string | X |
| `총학점` | 총학점 만점 기준 | 위와 같은 셀에 포함됨 | string | X |
| `병역` | 병역의무 | (샘플엔 없지만 요구사항에 명시됨) | string | X |
| `기술역량` | 직무별 보유 기술/툴/수준 | "직무"/"보유 기술, 툴"/"역량" 표, "(예시) Back-end" 행은 견본 | list[{직무, 툴, 수준}] | **O** |
| `프로젝트경험` | 프로젝트명/역할/사용기술/기간 | "프로젝트명"/"역할"/"사용기술"/"기간" 표, "(예시) AI 챗봇 개발" 행은 견본 | list[{프로젝트명, 역할, 사용기술, 기간}] | **O** |
| `기타특이사항` | 수상/인턴/협업 경험 등 | "기타 특이사항" | string | X |
| `포트폴리오링크` | Github/Notion/Blog 등 | "포트폴리오" | string | X |
| (별도) `자소서QnA` | 자소서 질문-답변 쌍 | "지원동기", "직무 관련 경험" 등 | list[{질문, 답변}] | **O** |

> 이 목록은 **초안**입니다. 실제로 다른 라벨을 만나면 이 사전에 새 키를 추가하세요 (지우거나 이름을 바꾸지 말고 **추가**만 — A/B 양쪽 다 갱신). 사전에 없는 라벨은 절대 억지로 기존 키에 끼워 맞추지 말고 `canonicalKey: null`로 남겨두세요 (§3 참고).

> **양식마다 다 나올 필요 없음**: 어떤 양식이 이 사전의 모든 필드를 다 요구할 거라고 가정하지 않습니다. B는 해당 양식에 **실제로 있는 라벨만** 처리하면 됩니다 — 없는 필드는 그냥 출력하지 않으면 됩니다. 반대로 사전에 없는 새 라벨(예: 취업 지원서의 "희망연봉")은 `canonicalKey: null`로 남기고, 자동 채움 대상에서 제외해 사용자가 직접 입력하게 합니다 (억지로 채우지 않는다는 grounding 원칙과 동일).

> **동의어 매핑 vs 분리 필드 주의**: "성명"과 "이름"처럼 표현만 다르고 같은 개념이면 둘 다 같은 canonicalKey(`이름`)로 매핑되어야 합니다 (Instruct가 의미로 판단하므로 가능). 단, 일부 양식은 "성"(surname)과 "이름"(given name)을 **별도 칸**으로 나누기도 하는데, 이 경우 둘을 `이름`으로 뭉치면 안 됩니다. 그래서 **B에게는 라벨을 하나씩이 아니라 표 전체 목록을 한 번에 줘서**, "이름" 옆에 "성"이 별도로 있는지 문맥으로 판단하게 해야 합니다.

---

## 2. 에이전트 A — 이미 작성된 문서 이해

### 목적
사용자가 업로드한 **이미 값이 채워진** 문서(이력서, 자소서, 자격증/어학성적 증명서 등)에서 정보를 뽑아, 위 필드 사전의 키로 정규화해 개인 DB(Firestore)에 저장 가능한 형태로 만든다.

### Studio 파이프라인
`Parse → Classify → Extract`

- **Classify 문서종**: `이력서 / 자소서 / 어학성적 / 자격증 / 대외활동증명서 / 기타`
  > **2026-08-23 실사용 확인**: 실제로는 이 목록에 없는 값("재학증명서", "수료증" 등)도 반환되고
  > 있고, 프론트는 이걸 자유 문자열로 받도록 이미 고쳐놨습니다(`DocumentCategory` 타입) — Classify
  > 라벨을 이 6개로 제한하려고 애쓸 필요 없습니다, 문서 실제 종류를 자연스럽게 반환하면 됩니다.
  >
  > ⚠️ **[긴급, 확인된 버그] 실제 자기소개서 문서 3건("현대카드 자기소개서.pdf", "KIDP
  > 자기소개서.pdf", "제일기획 자기소개서.pdf")을 우리 코드 거치지 않고 Upstage API에 직접
  > 넣어봤더니 3건 다 `"자소서"`가 아니라 `"기타"`로 분류됐습니다.** 그래서 자소서 Extract(질문/답변
  > 리스트) 자체가 실행이 안 되고, 결과적으로 기능2(유사 자소서 답변 찾기)가 이 3개 파일에서는
  > 아무 데이터도 못 가져옵니다. 파일명에 "자기소개서"가 명시돼 있고 내용도 명백한 자소서 Q&A
  > 형식인데 "기타"로 새는 걸 보면 Classify 프롬프트가 "자소서"를 판단하는 기준이 너무 좁거나
  > 이력서에 포함된 자소서 섹션 위주로만 학습/지시된 것 같습니다. **데모에서 기능2를 보여주려면
  > 이 Classify 정확도부터 고쳐야 데이터가 존재합니다** — 시간 되면 최우선으로 봐주시면
  > 좋겠습니다.
- **문서종별 Extract 스키마**를 따로 만들어야 함 (하나로 퉁치지 말 것):
  - **이력서**: §1의 canonicalKey들을 스키마 항목명으로 그대로 사용 (기술역량·프로젝트경험은 List 타입 테이블로)
  - **자소서**: `자소서QnA` — List 타입 테이블, `{질문, 답변}`
  - **자격증/어학성적**: `{명칭, 발급기관, 취득일, 만료일}`

  > ⚠️ **남은 요청사항 (2026-08-23 기준, 아직 구현 안 됨)**: 지금 실제 배포본은 이력서가 아닌
  > 문서에 대해 위 문서종별 Extract를 하지 않고, **Classify 라벨 문자열 하나만** 반환하고
  > 있습니다(정상 동작으로 확인함, QA_수정요구사항.md §4-5). 즉 자격증/어학성적/증명서류의
  > 취득일·만료일이 전혀 안 나오고 있어서 **기능3(만료일 관리)이 막혀있는 상태**입니다. 시간
  > 되시면 이력서 외 문서종에도 위 `{명칭, 발급기관, 취득일, 만료일}` 스키마의 Extract를 추가
  > 부탁드립니다 — 급하진 않고, 시간이 없으면 이번 데모에서는 기능3을 스코프 아웃해도 됩니다.

### 출력 예시 (이력서 1건 처리 시)

```json
{
  "sourceDocumentId": "doc_abc123",
  "docType": "이력서",
  "fields": {
    "이름": "홍길동",
    "생년월일": "2003년 05월 12일 ( 만 23세 )",
    "성별": "남",
    "주소": "서울특별시 ...",
    "연락처": "010-1234-5678",
    "이메일": "hong@example.com",
    "학교": "숙명여자대학교",
    "전공": "컴퓨터과학전공",
    "복수전공": null,
    "학년": "4학년",
    "평균학점": "4.1",
    "총학점": "4.5",
    "기술역량": [
      { "직무": "Back-end", "툴": "Python / Docker / Django", "수준": "중급" }
    ],
    "프로젝트경험": [
      { "프로젝트명": "AI 챗봇 개발", "역할": "백엔드 개발", "사용기술": "Python, FastAPI", "기간": "'25.3.~6." }
    ],
    "기타특이사항": "정보처리기사 취득",
    "포트폴리오링크": "github.com/hong"
  }
}
```

자소서는 별도 출력:
```json
{
  "sourceDocumentId": "doc_def456",
  "docType": "자소서",
  "자소서QnA": [
    { "질문": "지원동기", "답변": "..." },
    { "질문": "직무 관련 경험", "답변": "..." }
  ]
}
```

### 만족 조건 (체크리스트)
- [ ] 모든 필드명이 §1 필드 사전의 canonicalKey와 정확히 일치 (다른 표현이면 사전에 추가하고 그 키로)
- [ ] 문서에 없는 정보는 `null`/빈 값으로 — **절대 지어내지 않음** (grounding 원칙, 트랙 심사 기준이기도 함)
- [ ] 모든 출력에 `sourceDocumentId` 포함 (§4.5 출처 표시 원칙 — 이 값이 없으면 Dev B가 UI에 출처를 못 보여줌)
- [ ] 반복 항목(기술역량/프로젝트경험/자소서QnA)은 개수 제한 없이 리스트로 — 문서에 3개 있으면 3개 다 반환
- [ ] Classify가 문서종을 잘못 판단하는 케이스(예: 자소서인데 이력서로 분류)를 몇 개 실제 문서로 테스트

### 엣지 케이스
- 같은 문서에 같은 필드가 여러 번 언급되면(예: 자소서 안에 이메일이 또 나옴) 첫 번째 유효한 값 사용
- 표현이 애매한 값(예: "졸업예정")은 원문 그대로 저장, 정규화 억지로 하지 않음

---

## 3. 에이전트 B — 빈 양식 이해

### 목적
사용자가 채워야 할 **빈 양식**(회사/기관마다 전혀 다른 포맷, 사전에 알 수 없음)에서 "어떤 정보를 요구하는 빈칸이 어디 있는지"를 파악한다.

### 왜 전처리가 필요한가
Upstage Parse는 문서를 HTML/Markdown **사본**으로 바꿔줄 뿐, 원본 docx 파일의 정확한 좌표(몇 번째
표·행·병합그룹)는 주지 않습니다. 그런데 우리는 "채워야 할 원본 파일"에 값을 다시 써넣어야 하므로,
python-docx로 원본을 직접 열어서 얻는 좌표가 필요합니다. 게다가 "이 셀이 병합돼서 몇 칸짜리 빈칸인지"는
XML 구조를 그대로 읽으면 100% 정확하게 나오는, **AI가 아니라 코드가 결정적으로 풀 문제**입니다 — AI에게
시키면 부정확할 수 있고 Studio 실행 횟수만 낭비합니다. 그래서 구조 감지(전처리)는 코드, 의미 해석(라벨이
뭘 뜻하는지)만 에이전트 B(AI)가 담당하도록 역할을 나눴습니다.

> 참고로 **에이전트 A는 이 전처리가 필요 없습니다** — A는 이미 작성된 문서를 읽기만 하면 되고 원본에
> 다시 쓸 필요가 없어서 Parse의 HTML/Markdown 출력만으로 충분합니다. 원본 파일에 write-back이 필요한
> 건 B뿐이라 B만 python 전처리가 필요합니다.

### 전처리 (코드, AI 아님 — 이미 검증됨, `IFactory개발명세초안.md` §4.6 참고)
python-docx로 표를 순회하며, 병합된 셀을 **셀 객체 identity(`id(cell._tc)`) 기준으로 그룹핑**해서 (라벨후보, 값셀위치, 힌트텍스트, colspan) 튜플을 뽑는다. 실제 샘플로 검증된 결과:

```
('생년월일(나이)', colspan=2) → ('0000년 00월 00일 ( 만 00세 )', colspan=5)
('성명', colspan=2)          → ('', colspan=5)
```

이 전처리 결과(라벨/힌트 리스트)가 **에이전트 B(AI)의 입력**입니다. ⚠️ **라벨을 하나씩 개별 호출하지 말고, 표 전체 라벨 목록을 한 번에 통째로** 전달하세요 — "성"과 "이름"이 별도 칸으로 나뉜 양식인지 같은 "이름" 칸인지는 주변 라벨과 비교해야 판단할 수 있습니다 (§1 하단 참고).

> **실제 입력 예시 파일**: `fixtures/agent-b-sample-input.json` — 오늘 검증한 실제 지원서 양식 5개 표 전체를 위 전처리 알고리즘으로 돌린 원본 출력입니다(파이썬 코드는 Dev A가 직접 작성 예정이라 여기엔 결과만 있음). 각 표의 각 행이 `{ mergedGroupIndex, text, colspan }` 배열로 들어있고, 이게 곧 에이전트 B가 받는 원재료입니다. 이 파일을 그대로 Studio Instruct 프롬프트 입력으로 넣고 아래 "출력 예시" 형태가 나오는지 바로 테스트해볼 수 있어요.
>
> 참고로 이 예시 파일을 보면 이번 양식엔 두 가지 종류의 표가 섞여 있다는 게 보입니다: (1) `tableIndex 1`처럼 "라벨→빈칸" 패턴이 명확한 표, (2) `tableIndex 4`(자격 체크리스트)처럼 라벨-값 페어가 아니라 그냥 데이터 표(체크박스형)인 것. 에이전트 B는 후자를 억지로 필드 매핑하지 말고 `canonicalKey: null`로 넘기거나 "프로필 데이터로 채울 대상이 아님"으로 표시해야 합니다 (아래 만족 조건 참고).

> ⚠️ **`fixtures/agent-b-sample-input.json`을 Studio UI에 파일로 업로드하려고 하면 안 됩니다** —
> Studio가 지원하는 업로드 포맷은 PDF·이미지·오피스(DOC/DOCX/PPT/PPTX/XLS/XLSX)·HWP/HWPX·이메일/웹뿐이라
> **JSON은 지원되지 않습니다** (실제로 시도해서 확인됨). 원본 docx 파일만 업로드해야 합니다.
>
> **[정정, 2026-08-23]** 한때 이 자리에 "API 호출 시 python-docx 전처리 JSON을 `input_text`로
> 같이 보내면 에이전트 B가 설명문으로 응답하니 프롬프트를 고쳐야 한다"는 내용이 있었는데,
> **틀린 진단이라 삭제했습니다**. 팀원 프롬프트는 처음부터 문제없었고, 원인은 개발자(Claude)
> 쪽 API 호출 코드가 그 JSON을 `input_text`로 주입한 것이었습니다(모델이 "이 데이터를 분석해서
> 설명해줘"로 오인). 그 주입 코드를 제거하니 파일만 보냈을 때 정상 JSON이 나오는 것으로
> 확인됨(A/B 라이브 테스트). **에이전트 B 프롬프트는 지금 그대로 두면 됩니다** — 전처리 JSON을
> Studio에 별도로 넣어줄 필요 자체가 없어졌습니다.

### 에이전트 B(AI)가 판단할 것
전처리된 (라벨, 힌트) 목록을 받아서:
1. 각 라벨을 §1 필드 사전의 **canonicalKey로 매핑** (매칭 안 되면 `null` + 원문 라벨 보존, 억지로 끼워 맞추지 않음)
2. 힌트 텍스트가 있으면 **값의 형식**을 판단 (예: "0000년 00월 00일 ( 만 00세 )" → "YYYY년 MM월 DD일 ( 만 NN세 )" 포맷)
3. **반복 가능한 리스트형 필드인지** 판단 — "(예시)" 라벨이 붙은 견본 행 + 그 아래 빈 행이 있으면 리스트형 (기술역량, 프로젝트경험처럼)
4. "(예시)" 라벨이 붙은 행 자체는 **덮어쓰기 대상**으로 별도 표시 (실제 값이 아니라 형식 안내이므로)

### 출력 예시

```json
[
  {
    "rawLabel": "성명",
    "canonicalKey": "이름",
    "formatHint": null,
    "isRepeatable": false,
    "cellRef": { "table": 1, "row": 0, "mergedGroupIndex": 3 }
  },
  {
    "rawLabel": "생년월일(나이)",
    "canonicalKey": "생년월일",
    "formatHint": "0000년 00월 00일 ( 만 00세 )",
    "isRepeatable": false,
    "cellRef": { "table": 1, "row": 1, "mergedGroupIndex": 3 }
  },
  {
    "rawLabel": "프로젝트명 / 역할 / 사용기술 / 기간",
    "canonicalKey": "프로젝트경험",
    "formatHint": null,
    "isRepeatable": true,
    "exampleRowToIgnore": { "table": 1, "row": 18 },
    "targetRowTemplate": { "table": 1, "row": 19 }
  },
  {
    "rawLabel": "확인 여부 (해당/비해당 체크)",
    "canonicalKey": null,
    "formatHint": null,
    "isRepeatable": false,
    "note": "체크리스트 성격 — 프로필 데이터로 채울 대상이 아닐 수 있음, 사람이 확인 필요"
  }
]
```

> ⚠️ **실제 연동 테스트에서 확인된 버그 (2026-08-23)**: 실제 배포된 에이전트 B가 "성명" 라벨에 대해
> `cellRef.mergedGroupIndex`를 **라벨 자기 자신의 위치**(예시에서 2)로 반환하는 경우가 있었음 — 원래
> 의도는 **값을 써넣을 빈 칸의 위치**(예시에서 3)를 가리켜야 함. 지금은 `python/docx_tools.py`의
> `fill_values()`에 "지정된 인덱스가 그 행의 마지막 그룹이 아니면 마지막 그룹(값 칸)으로 자동 보정"하는
> 방어 로직을 넣어서 우회했지만, 원인은 Studio Instruct 프롬프트가 "라벨 다음에 오는 칸"이 아니라
> "라벨이 있는 칸"을 가리키도록 판단하고 있어서로 추정됨. 프롬프트에서 "cellRef는 값을 써넣을 빈
> 칸(라벨 바로 다음/마지막 병합그룹)을 가리켜야 하며, 라벨이 있는 칸 자체를 가리키면 안 된다"를
> 명시적으로 강조하면 개선될 것으로 보임.
>
> **또한 canonicalKey가 한글이 아니라 영어 snake_case로 나오고 있음** (`korea_name`,
> `date_of_birth`, `gender`, `address` 등 — 에이전트 A와 일치, 아래 §1 사전은 한글로 작성된 초안이라
> 실제와 다름). 두 에이전트가 서로 일치하기만 하면 매칭엔 문제없어서 프론트/백엔드 쪽에서 영어 키
> 그대로 쓰도록 맞춰뒀습니다 — §1의 한글 사전은 "개념 정리용 초안"으로 참고하되, **실제 계약은
> `web/src/lib/agentMapping.ts`의 `PROFILE_FIELD_MAP`이 최신 소스**입니다:
> `korea_name, date_of_birth, gender, address, phone_number, email_address, university, major,
> my_gpa, avg_gpa, military, linkedin_url` (+ 자소서는 `self_introduce: [{question, answer}]`).
>
> **[정정, 2026-08-23]** university(학교)와 major(전공)는 **분리된 별도 필드**입니다(예전엔 `major`
> 하나에 합쳐 쓴 적도 있었는데 지금은 나눠서 옴). gpa도 `my_gpa`(본인 평균학점)/`avg_gpa`(총학점
> 만점 기준)로 분리돼서 옵니다. 자소서 Q&A 필드명은 `self_introduction`이 아니라 **`self_introduce`**이니
> 철자에 주의하세요 (실제로 이 이름으로 와야 프론트 매핑 코드가 인식합니다).

### 만족 조건 (체크리스트)
- [ ] `canonicalKey`가 §1 사전과 정확히 일치 (Agent A와 동일한 사전 사용 — 매칭 단계의 전제조건)
- [ ] `cellRef`로 원래 셀 위치를 정확히 복원 가능 (python-docx로 다시 쓰기 위해 필수)
- [ ] "(예시)" 견본 행은 `exampleRowToIgnore`로 명시, 실제 값 기입 대상(`targetRowTemplate`)과 구분
- [ ] 매칭되는 canonicalKey가 없는 라벨은 `null` + `rawLabel` 보존 (프로필과 무관한 체크리스트/서명란 등도 있을 수 있음 — 이런 건 억지로 채우지 말 것)
- [ ] 표 기반 양식 최소 2개(오늘 받은 샘플 포함)로 실제 테스트

### 자소서형 빈칸(중요 — 일반 필드와 다르게 처리)
실제 샘플 양식을 보면 "지원동기", "직무 관련 경험" 같은 자소서 항목도 구조적으로는 일반 필드와
똑같이 **라벨 + 힌트(글자수 제한) + 빈 칸**으로 되어 있습니다. 다만 처리 방식이 다릅니다:

- 일반 필드(성명, 생년월일 등)는 라벨을 §1 사전의 canonicalKey로 **정규화**해서 매핑
- 자소서형 라벨은 정규화하지 말고 **라벨 텍스트 자체를 질문 문장으로 그대로 보존**해서 반환 (canonicalKey는 `자소서QnA`로 고정, `questionText`에 원문 라벨을 그대로 담음)
- 판별 기준(예시): 힌트가 "공백 포함 500자 이내"처럼 글자수 제한이거나, 빈 칸이 여러 줄(장문형)이면 자소서형으로 판단

```json
{
  "canonicalKey": "자소서QnA",
  "questionText": "지원동기",
  "isRepeatable": false,
  "cellRef": { "table": 2, "row": 2 }
}
```

이 출력은 프론트의 자소서 문항 편집 UI(직접 입력 + 기존 자료에서 유사 질문 찾기 + 삽입하기, `web/src/components/EssayQuestionBlock.tsx`)가 그대로 받아서 쓰는 형태입니다 — `questionText`를 그 컴포넌트의 `question` prop에 넣으면 됩니다.

### 엣지 케이스
- 라벨이 두 칸에 나뉜 경우(예: "학과명"+"주전공") — 두 라벨을 합쳐서 하나의 canonicalKey로 판단
- 체크박스/서명란처럼 프로필 데이터와 무관한 빈칸 — `canonicalKey: null`로 두고 자동 채움 대상에서 제외
- 힌트 텍스트가 실제로는 "이미 채워진 값"인지 "형식 안내"인지 애매한 경우 — 숫자/날짜 패턴(0000, 00 등)이 포함되면 안내로 간주
- **"성명"/"이름"처럼 동의어인 라벨은 같은 canonicalKey로, "성"+"이름"처럼 별도 칸으로 분리된 경우는 별도 키로** — 표 전체 라벨을 한 번에 봐야 구분 가능 (위 §1 하단 참고)
- 이 양식에 사전(§1)에 없는 라벨이 나오면 억지로 기존 키에 끼워 맞추지 말고 `canonicalKey: null` + `rawLabel` 보존 — 나중에 사전에 새 키로 추가

---

## 4. 매칭 단계 (참고 — A/B가 아니라 이후 통합 코드가 담당)

A·B가 각자 완성되면, 아래는 **Dev A(팀원)가 이어서 구현**하는 부분입니다. A/B 자체 요구사항은 아니지만, 출력 형식이 이 단계를 염두에 두고 설계돼야 하므로 참고로 남깁니다.

1. `canonicalKey`가 정확히 일치 → 코드로 바로 값 매칭 (AI 호출 불필요)
2. `canonicalKey: null`이거나 애매한 경우만 → Solar Pro2에게 "이 라벨에 프로필의 어떤 데이터가 맞을지" 판단 요청 (호출 최소화 목적)
3. 리스트형 필드(기술역량/프로젝트경험)는 사용자의 `experiences` 중 적합한 항목을 골라 `targetRowTemplate` 개수만큼 매핑

---

## 5. 완료 정의 (Definition of Done)

**에이전트 A 완료 조건**
- [ ] 이력서/자소서/증명서 각각 최소 1건씩 실제 문서로 테스트 통과
- [ ] 출력이 §1 필드 사전 키와 100% 일치
- [ ] 모든 출력에 `sourceDocumentId` 포함
- [ ] 없는 정보를 지어낸 사례가 0건

**에이전트 B 완료 조건**
- [ ] 표 기반 양식 2개 이상(오늘 샘플 포함)으로 테스트 통과
- [ ] `cellRef`로 python-docx가 정확한 셀을 다시 찾을 수 있음을 확인
- [ ] "(예시)" 행 인식 및 구분 확인
- [ ] canonicalKey 매칭이 안 되는 라벨을 억지로 매칭한 사례가 0건
