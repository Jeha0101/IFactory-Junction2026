# IFactory 개발 명세 초안

> 이 문서는 개발 전 팀이 합의한 결정 사항을 기록하는 **살아있는 문서**입니다.
> **코드를 작성하기 전에 반드시 이 문서를 먼저 수정**하고, 새로 결정된 내용은 하단 "7. 결정 로그"에 날짜와 함께 남깁니다.
>
> ⏰ **남은 시간: 17시간** (2026-08-22 기준) — 4시간 뒤 러프 MVP, 12시간 뒤 발표 가능한 제품 목표.

---

## 0. 프로젝트 한 줄 요약

이력서/자기소개서 작성을 돕는 AI 에이전트. 동시에 사용자의 학력·경력·자격증·어학성적·대외활동 등 개인정보를 저장하는 **개인 DB** 역할을 겸한다. 핵심 신뢰 포인트: **AI가 지어낸 게 아니라 사용자의 실제 자료에서 나온 정보**임을 항상 출처와 함께 보여준다 (§4.5).

---

## 1. 대회 제약 조건 (Upstage Track Challenge)

- 트랙: *Building Practical AI Agent Implementation with Solar Pro2*
- 필수 기술: **Solar Pro2**
- 평가 기준(100점): Project Impact(25) / Technical Implementation(25) / Effective Use of Upstage API(25) / Innovation and Creativity(25)
- 제출물: GitHub 저장소, 프로토타입, 데모 영상(3-5분), 발표 PPT
- **크레딧: $50 확보** — 비용 걱정 없이 개발

### 1.1 Upstage 아키텍처 (공식 문서 기준)

- **Studio**: 노코드로 Parse → Classify → Extract → Instruct 노드를 파이프라인("에이전트")으로 조립
  - **Parse**: 문서 → 텍스트(HTML/Markdown) 변환
  - **Classify**: 문서를 사용자 정의 문서종으로 분류 (+ Split 모드로 한 파일 안 여러 문서 분할)
  - **Extract**: 스키마로 정의한 항목을 구조화 추출 (반복 항목은 List 타입 테이블로)
  - **Instruct**: 자연어 프롬프트로 판단/변환. `@항목명`으로 앞 Extract 결과 참조. "생성만" 또는 "생성+결정(분기)" 두 타입
- **호출 방식**: Studio에서 만든 에이전트를 **Agents API**로 코드에서 호출 — Files API로 업로드(`file_id`) → Jobs API로 `agent_id`+`config_id` 지정해 Job 생성 → 결과 조회(`include=last`/`all`, 항목별 신뢰도 `low`/`high` 포함)
- **무료 쿼터**: 에이전트당 문서 실행 10회까지 무료, 이후 PAYG (실패 실행은 과금 안 됨)
- **버전 관리**: 설정 수정 시 자동 Draft → 실행 시 Config 버전 확정. API는 특정 `config_id`를 고정 지정 가능해 운영(데모)이 안전

**채택 방향**: 표준 REST 엔드포인트를 직접 코드로 조합하는 대신, **Studio UI에서 에이전트를 만들어 Agents API로 호출**하는 방식 채택 (역할 분담은 §4.3).

---

## 2. 팀 / 개발 프로세스

- 개발자 2명
- **1단계**: Claude Code로 초기 스캐폴딩(구조, 기본 CRUD, 핵심 흐름) 완성
- **2단계**: GitHub에 올린 뒤 기능 단위로 나눠 세부 구현·수정
- **원칙**: 기능을 구현하기 전 이 문서를 먼저 갱신 → 그 다음 코드 작업

---

## 3. 주요 기능 명세

> **2026-08-23 진행률 체크포인트** (마감 5시간 전 기준, 실제 코드 확인함 — 상세 근거는 §7 결정로그 최신 항목):
> - 🟢 기능 1: 됨 (원본 서식 유지, 실사용 검증). 단 반복 표(경력 여러 건)는 문서 빈칸에 자동 삽입 안 되고 목록으로만 표시됨(3-6, 미해결)
> - 🟡 기능 2: 저장 파이프라인은 됨. **단 유사도 매칭이 명세대로(Solar Pro2 Instruct 의미 기반)가 아니라 `web/src/lib/matchQuestions.ts`의 임시 토큰-겹침 휴리스틱** — 실제 AI 매칭 아직 미구현
> - ⚪ 기능 2b: 후순위, 미착수
> - 🔴 기능 3: 사실상 미구현 — UI(D-day 표시)는 있지만 이력서 아닌 문서(재학증명서/수료증/자격증 등)에 취득일·만료일을 뽑아주는 Extract 스키마가 에이전트 A에 아직 없어서 `expiresAt`이 항상 비어있음
> - 🔴 기능 4: 미구현 — `linkedExperienceId` 필드만 있고 실제로 채워지거나 연결하는 로직/UI가 없음
> - 🔴 기능 7: 취사선택 UI 자체가 없음 — 경력 목록은 보이지만 제거/포함 토글이 없어 전부 강제 포함됨

### 기능 1. 이력서 빈칸 자동 채우기
- 사용자가 작성해야 할 이력서 **양식(DOCX)**을 업로드 — **어떤 양식이 올지 모름**, 특정 템플릿에 종속되지 않는 것이 이 솔루션의 핵심 (§4.6)
- 저장된 기본 정보로 빈칸을 자동 채움, 원본 서식 유지
- 채워진 값 중 profile 외 항목(경력/자격증 등)은 **출처 표시** (§4.5)

### 기능 2. 자기소개서 유사 답변 찾기
- 업로드 시점에 자소서를 Extract(List 타입 테이블)로 파싱해 `{questionText, answerText, sourceDocumentId}` 페어를 Firestore에 미리 구조화 저장
- 새 질문 입력 시: 저장된 질문 리스트를 Solar Pro2(Instruct)에 통째로 넣어 유사한 질문을 찾음
- 매치된 답변 **원문을 그대로** 제시 + 출처(원본 파일) 표시 — AI가 새로 생성하지 않음
- **매치가 3개 미만이거나 0개여도 억지로 채우지 않는다** — 있는 만큼만 보여주거나, 0개면 "아직 작성한 자기소개서가 없습니다" 안내

#### 기능 2b (후순위, 시간 남으면). 유사 답변 활용 방식 3가지 제공
매칭된 답변(최대 3개, 부족하면 있는 만큼)을 보여주고 사용자가 아래 중 선택:
1. **참고만 하기** — 3개를 보고 사용자가 직접 새로 작성
2. **그대로 채택** — 3개 중 하나를 선택해 그대로 넣음
3. **AI와 조합** — 3개를 재료로 LLM과 채팅하며 새 답변을 함께 생성

구현: top-1 대신 top-3 반환(Instruct "생성만" 타입, JSON 순위 리스트) + 옵션 3은 Studio 밖에서 Solar Pro2를 채팅형으로 직접 호출.
⚠️ 옵션 3은 원문 그대로 보여주는 기존 원칙과 달리 AI가 새로 생성하는 경로 — UI에서 "AI가 조합해 새로 작성함"으로 명확히 구분 표시.

### 기능 3. 증명서 날짜 관리
- 어학성적/자격증/대외활동증명서의 취득일·만료일 저장, 만료 임박 알림

### 기능 4. 증명서 파일 미리보기/링크
- 이력서에 기재된 자격/경력에 연결된 증명서 파일을 작성 화면에서 바로 미리보기·열람

### 기능 5 (향후, 미개발). 증명서 통합 PDF 추출
- 문서를 "파일 단위 + 메타데이터" 구조로 저장해 추후 조합(merge) 서비스만 추가하면 되도록 확장성 확보

### 기능 6 (향후, 미개발). 문서 자동 폴더 정리
- Firestore `documents`에 `category` 필드를 처음부터 둬서(Classify 결과 재사용) 나중에 분류 로직만 추가하면 되도록 함

### 기능 7. 경력/경험 취사선택 미리보기
- 최종 이력서 생성 전 미리보기에서 경력/경험 포함 여부를 사용자가 선택
- "경험" 데이터를 "프로젝트" 같은 다른 이름의 항목에도 매핑 가능 (§4.4 experiences 모델이 애초에 범용이라, Instruct가 프로젝트 성격인 것만 골라 대상 필드에 매핑 판단)

---

## 4. 아키텍처

### 4.1 스택

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | Next.js (React+TS) → **Vercel** | 무료 배포, 빠른 데모, 컴포넌트 단위 분업 |
| 백엔드 | FastAPI (Python) → Railway/Render | `python-docx`로 DOCX 원본 서식 유지 편집, Upstage 키를 서버에서만 관리 |
| DB/스토리지 | **Firebase (Firestore + Storage)** | 팀이 익숙함. Firestore: 구조화 데이터, Storage: 원본 파일 |
| 인증 | **없음 (단일 데모 계정 하드코딩)** | 시간(17h) 절대 부족 — 필요시 나중 추가 |
| 데모 데이터 | 더미(가짜) 개인정보만 사용 | 민감정보(생년월일, 병역 등) 보호 |
| Upstage 연동 | Studio 에이전트 + Agents API | §1.1 |

### 4.2 업로드 시 파싱 전략 — "미리 다 구조화해서 저장"

**결정**: 모든 파일은 업로드 시점에 Studio 에이전트(Parse→Classify→Extract)를 1회 호출해 구조화된 정보를 Firestore에 저장. 이후 이력서를 만들 때는 저장된 데이터만 읽고 **재파싱하지 않는다**.

**이유**: (1) 매번 재파싱하면 무료 쿼터(에이전트당 10회)·속도 둘 다 손해 (2) Classify 결과를 `category` 필드에 저장해두면 기능 6이 절반은 이미 구현됨 (3) 원본은 Storage에, 구조화 데이터는 Firestore에 캐싱하는 구조라 기능 5(PDF 병합)도 지금 구조 그대로 확장 가능

Classify 문서종: **이력서 / 자소서 / 어학성적 / 자격증 / 대외활동증명서 / 기타** 로 확정.

> **2026-08-23 실사용 정정**: 실제 배포된 에이전트 A는 위 6종 외에도 "재학증명서", "수료증" 등
> 목록에 없는 값을 자유롭게 반환함 — Classify 결과를 고정된 enum으로 강제하지 않고 **자유
> 문자열로 받도록** 프론트를 수정함(`DocumentCategory` 타입, `web/src/types/index.ts`).
> 또한 에이전트 A는 **이력서일 때만** 개인정보 Extract용 JSON을 반환하고, 이력서가 아닌
> 문서는 분류 라벨 문자열 하나만 반환함(정상 동작, §7 참고) — 자격증/어학성적/증명서류의
> 취득일·만료일(기능3용)을 뽑아주는 문서종별 Extract 스키마는 **아직 없음** (Agent_요구사항명세서.md §2 참고, 남은 요청사항).

### 4.3 분업 경계 (개발자 2명)

- **Dev A(팀원) — AI/문서처리 엔진**: 현재 **에이전트 A·B 구현 중** (아래 §4.7, 상세 요구사항은 별도 문서 `Agent_요구사항명세서.md`)
- **Dev B(나) — 데이터/프론트엔드**: Firestore 스키마·CRUD, Storage 업로드, 기능 3·4·7, Next.js 전반, Vercel 배포

API 스펙(요청/응답 JSON)만 먼저 합의하면 둘 다 바로 병렬 착수 가능.

#### Dev A(팀원)의 작업 순서

1. **에이전트 A** — 이미 작성된 문서(이력서/자소서/증명서) 이해 → 구조화 데이터 (진행 중)
2. **에이전트 B** — 빈 양식 이해 → 필드/빈칸 구조 파악 (진행 중)
3. **매칭/리컨실 로직** — A의 (canonicalKey→값) 사전과 B의 (canonicalKey→빈칸위치) 목록을 대조해 실제로 빈칸에 넣을 값을 확정 (§4.7 "매칭 단계")
4. **python-docx write-back** — 3번 결과를 §4.6 파이프라인으로 실제 파일에 기입 (구조 감지 스크립트는 이미 검증 완료, 이어서 통합)
5. **기능 2(자소서 매칭) 연동** — 에이전트 A가 이미 자소서 Q&A도 추출하므로(§4.7), "질문 유사도로 최적 답변 찾기"는 3번의 매칭 로직과 패턴이 동일함(리스트에서 최적 후보 고르기) → 재사용 가능
6. 시간 남으면 기능 2b(top-3 + AI 채팅 조합)

1~2번이 끝나는 대로 3번(매칭)으로 바로 이어가는 걸 추천 — 둘 다 없으면 검증이 안 되고, 3번이 실질적으로 "에이전트"라는 걸 처음 증명하는 단계라 데모 임팩트도 큼.

### 4.4 데이터 모델 (Firestore, 초안)

```
users/{demoUser}
 ├─ profile: { 이름, 생년월일, 성별, 주소, 연락처, 이메일, 학교, 전공, 복수전공(nullable), 학년, 평균학점, 총학점, 병역, 포트폴리오링크 ... }  ← 출처 표시 불필요 (§4.5). 필드 목록은 §4.7 필드 사전과 동일해야 함
 ├─ experiences/{expId}: {
 │    기관, 역할, 기간, 설명,
 │    type (선택: project/work/activity/award — 명확하면 Extract 단계에서 태깅, 애매하면 Instruct가 그때그때 판단),
 │    sourceDocumentId   ← 출처 표시용
 │  }
 ├─ documents/{docId}: {
 │    fileUrl (Storage 경로),
 │    category (이력서/자소서/어학성적/자격증/대외활동/기타),  ← Classify 결과, 기능6 확장 대비
 │    acquiredAt, expiresAt,                              ← 기능3용
 │    linkedExperienceId (nullable)                        ← 기능4용
 │  }
 └─ coverLetterAnswers/{answerId}: { questionText, answerText, sourceDocumentId }
```

### 4.5 데이터 신뢰성 — 출처 표시 원칙

**요구사항**: 이 프로젝트의 핵심 신뢰 포인트는 "AI가 지어낸 게 아니라 사용자의 실제 자료에서 나온 정보"라는 것. 파싱/추출로 얻은 모든 데이터는 **어느 파일에서 나왔는지(`sourceDocumentId`)**를 저장하고 UI에 노출해야 한다.

- **출처 표시 필요**: experiences, coverLetterAnswers, documents에서 파생된 모든 값 (자격증 취득일, 프로젝트 경험, 자소서 답변 등)
- **출처 표시 불필요**: profile의 기본 인적사항(이름, 생년월일, 연락처 등) — 사용자가 직접 입력한 값이라 출처가 자명함
- UI 반영: 기능1(빈칸 채우기)·기능7(미리보기)에서 채워진 값 옆에 "출처: [파일명]" 표시, 기능2 답변에도 출처 파일명 표시

### 4.6 DOCX 빈칸 감지 방식 (실제 샘플로 검증 완료)

실제 지원서 양식(표 기반)으로 스파이크 테스트 완료. **핵심 발견**: python-docx는 병합된 셀을 "같은 셀 객체가 반복되는" 방식으로 노출한다. 텍스트가 아니라 **셀 객체 identity(`id(cell._tc)`) 기준으로 그룹핑**하면, 하드코딩 없이 "라벨 그룹 → 값 그룹" 쌍을 정확히 뽑아낼 수 있다.

```
('생년월일(나이)', colspan=2) → ('0000년 00월 00일 ( 만 00세 )', colspan=5)   # 힌트 텍스트가 있는 빈칸
('성명', colspan=2)          → ('', colspan=5)                             # 완전 빈칸
```

**파이프라인** (표 기반 양식 한정, 이번 대회 범위):
1. python-docx로 모든 표를 순회, 병합 그룹 기준 (라벨, 값셀위치, 힌트텍스트) 추출 — AI 불필요, 코드로 결정적 처리
2. 라벨+힌트 목록을 Solar Pro2(Instruct)에 프로필 데이터와 함께 전달 → 라벨이 어떤 프로필 필드에 대응하는지, 값을 어떻게 포맷할지 판단
3. python-docx로 판단된 값을 정확한 셀에 재기입 (서식 유지)
4. "(예시)" 라벨이 붙은 행은 무시 대상으로 별도 지시

**의의**: 라벨 텍스트를 미리 알 필요 없이 구조 패턴만으로 임의의 표 기반 docx에 적용 가능 — "어떤 양식이든 대응"이라는 핵심 요구사항 충족.

**범위 제한**: 표 기반 양식만 검증. 밑줄/공백형 문단 기반 양식은 시간 남으면 추가 검증.

### 4.6-1 [폐기됨] 최종 문서 WYSIWYG 편집 → 좌표 기반 필드값 맵 방식으로 전환

> **2026-08-23 정정**: 아래에 적었던 "`docx-preview` + `contentEditable`로 문서를 직접 타이핑
> 편집" 방식은 **실사용 QA에서 치명적 결함으로 드러나 폐기**했습니다. 문제: 미리보기(docx→HTML
> 변환)와 다운로드(HTML→docx 재변환)가 서로 다른 손실 변환 경로를 타면서, 다운로드한 최종
> 파일이 원본과 심하게 달라지는 문제가 발생했습니다(QA_수정요구사항.md §3-7, "가장 치명적"
> 등급). 아래 원안은 기록 목적으로만 남겨둡니다.
>
> **현재 아키텍처**: `DocxLivePreview`는 읽기 전용 렌더러로만 씀. 실제 값은 에이전트 B의
> `cellRef` 좌표를 키로 하는 `fieldValues: Record<string, string>` 맵(`ResumeDraft.fieldValues`,
> `web/src/types/index.ts`)에 저장하고, 사용자는 이 맵에 대응하는 일반 입력 폼(텍스트박스)으로
> 값을 편집합니다. 미리보기 새로고침(`/api/render-docx`)과 실제 다운로드가 **동일한
> `fill_values()` 파이프라인**을 한 번만 타므로 손실 변환이 원천적으로 없습니다. 분석(`/api/analyze-form`,
> 에이전트 B 호출)과 렌더링(`/api/render-docx`, 순수 python-docx)도 별도 라우트로 분리해서
> 렌더링 경로에는 에이전트 호출이 섞이지 않게 했습니다.
>
> 원안이 검증했던 사실 자체(`docx-preview`로 서식 깨짐 없이 렌더링 가능)는 여전히 유효하고,
> 지금도 읽기 전용 미리보기 용도로 그대로 재사용 중입니다.

### 4.7 에이전트 A/B 공용 필드 사전 & 매칭 단계

**핵심 설계 원칙**: 에이전트 A(이미 작성된 문서 이해)와 에이전트 B(빈 양식 이해)가 **같은 필드 이름(canonical key) 사전**을 쓰면, 이후 매칭은 AI 없이 단순 키 대조로 끝난다. 다르면 매번 애매한 텍스트 매칭을 해야 해서 느리고 부정확해진다. 상세 계약은 `Agent_요구사항명세서.md` 참고, 여기선 개요만.

**필드 사전(초안, 실제 샘플 양식 기준)**: 이름, 생년월일, 성별, 주소, 연락처, 이메일, 학교, 전공, 복수전공, 학년, 평균학점, 총학점, 병역, 기술역량(list), 프로젝트경험(list), 기타특이사항, 포트폴리오링크. 자소서는 별도로 {질문, 답변} 리스트.

> **2026-08-23 정정**: 위 한글 사전은 "개념 정리용 초안"일 뿐, **실제 두 에이전트가 주고받는
> 계약은 영어 snake_case 키**입니다(`korea_name`, `date_of_birth`, `gender`, `address`,
> `phone_number`, `email_address`, `university`, `major`, `my_gpa`, `avg_gpa`, `military`,
> `linkedin_url`, `self_introduce[]` 등). 실제 목록과 최신 상태는 `Agent_요구사항명세서.md`
> §2 하단 및 `web/src/lib/agentMapping.ts`의 `PROFILE_FIELD_MAP`이 최신 소스입니다.

**매칭 단계** (A·B 완성 후):
1. canonicalKey가 정확히 일치 → 코드로 바로 매칭 (AI 불필요)
2. canonicalKey가 없거나(`null`) 애매함 → 그때만 Solar Pro2에게 판단 요청 (호출 최소화)
3. 리스트형 필드(기술역량/프로젝트경험)는 사용자의 `experiences` 중 적합한 항목을 골라 순서대로 매핑 (§3 기능7의 "경험→프로젝트" 매핑도 이 단계에서 처리)

---

## 5. 타임라인 — 남은 17시간 기준

| 구간 | 목표 | 내용 |
|---|---|---|
| **0~1h** | 착수 | Firebase 프로젝트 생성, Studio 에이전트 초안 1개, Next.js+FastAPI 레포 스캐폴딩, API 스펙 합의 |
| **1~4h** | 🎯 러프 MVP | 기능 1·2·7 각각 "한 번은 성공적으로 동작"하는 수준. UI는 러프해도 됨 |
| **4~12h** | 🎯 발표 가능한 제품 | Firestore 정식 연동, UI 완성, 배포, happy path 1개는 완벽 동작. 시간 남으면 기능 3·4·2b 추가 |
| **12~17h** | 제출 준비 | 데모 영상(3-5분), PPT, 버그 파이어파이팅, README |

**우선순위**: Core = 기능1,2,7 / Nice to have = 기능3,4 / 후순위(시간 남으면) = 기능2b / 이번 대회 범위 밖 = 기능5,6

> **2026-08-23 갱신 — 마감 5시간 전 실제 상황**: 위 17시간 타임라인은 이미 지났음. 실제로는
> 기능1만 완성이고 기능2는 절반(매칭이 가짜 휴리스틱), 기능7은 핵심(취사선택)이 아예 없음,
> 기능3·4는 사실상 미착수(§3 상단 체크포인트). 남은 5시간엔 새 범위 확장보다 **데모 시나리오가
> 실제로 통과하는 happy path 하나를 완성/검증하는 것**을 최우선으로 함 — 어떤 기능을 그 안에
> 넣을지는 진행하면서 이 문서에 계속 기록.

---

## 6. 다음으로 의논해야 할 것

- [ ] **실제 데모용 샘플 파일 준비**: 더미 이력서 3종, 자소서 샘플 등 — 지원서 양식(표 기반)은 확보됨
- [ ] **문단 기반(밑줄/공백형) 양식 대응**: 이번 대회 범위에서 다룰지 — 표 기반 양식 우선 완성 후 시간 되면 스파이크

시간이 없으니 답변 기다리지 말고 각자 담당 영역에서 스파이크로 바로 결정하고 이 문서에 기록하는 걸 추천합니다.

---

## 7. 결정 로그

- **2026-08-22 오전**: 초안 작성 — 기능 1~7 정의, 개발 프로세스(2인, Claude Code→GitHub 분업) 확정
- **2026-08-22 오후**: DB Supabase→**Firebase**, 인증 생략(단일 데모 계정), Studio Agents API 연동 방식 확정, $50 크레딧 확보, 업로드 시 전량 파싱·구조화 전략 확정
- **2026-08-22 오후**: 실제 양식 샘플로 DOCX 빈칸 감지 스파이크 검증 완료(§4.6), 자소서 Q&A 추출은 Extract(List 타입)로 확정, "채우기" 실행 주체가 python-docx임을 명확화
- **2026-08-22 저녁**: 경험→프로젝트 매핑 가능 확인(§3 기능7), 기능2b(유사 답변 3안 제시: 참고/채택/AI조합) 후순위 요구사항 추가, 매칭 부족 시 억지 생성 금지 원칙 명시, 데이터 출처 표시 원칙 추가(§4.5)
- **2026-08-22 밤**: 이력서 채우기 파이프라인을 에이전트 A(기작성 문서 이해)/B(빈 양식 이해)/매칭(A·B 대조) 3단계로 구조화, 공용 필드 사전 도입(§4.7), 팀원(Dev A)이 A·B 구현 중임을 반영해 이후 작업 순서 정리(§4.3). 팀원 전달용 `Agent_요구사항명세서.md` 별도 작성.
- **2026-08-22 밤**: 양식마다 필드 개수/종류가 달라도 되도록 사전을 growable하게 설계(없는 필드는 출력 안 함, 새 필드는 null로 남김) 명시. "성명"/"이름" 동의어 매핑은 가능하나 "성"+"이름" 분리형 양식과 혼동하지 않도록 B에게 라벨 목록을 한 번에 전달해야 함을 `Agent_요구사항명세서.md`에 반영.
- **2026-08-22 밤(늦게)**: 왜 에이전트 B 전에 python 전처리가 꼭 필요한지(Parse는 원본 좌표를 안 줌, write-back엔 python-docx 객체 좌표 필요) `Agent_요구사항명세서.md`에 명시. 실제 샘플 양식 5개 표 전체를 전처리 알고리즘으로 돌린 원본 출력을 `fixtures/agent-b-sample-input.json`으로 저장 — 팀원이 파이썬 스크립트 완성 전에도 에이전트 B 프롬프트/스키마 개발을 바로 시작할 수 있음.
- **2026-08-22 밤(늦게)**: 프론트 UX 대폭 수정 — 내정보/서류함에서 수동 입력·분류 폼 제거(업로드만, 분류/추출은 에이전트 A), 이력서 미리보기의 경력 선택 UI 제거(전부 기본 포함), CTA형 진입("이력서 양식을 업로드해서 이력서 작성을 시작하세요!")으로 변경. 자소서 문항 편집 UI(직접입력+유사답변3개+삽입하기) 구현. `docx-preview`로 실제 문서 WYSIWYG 편집 가능함을 실제 샘플로 검증(§4.6-1) — 문서 내보내기는 향후 과제로 분리.
- **2026-08-22~23**: 팀원이 완성한 에이전트 A/B API 실연동. 실사용 QA 중 다운로드 파일이 원본과 심하게 달라지는 치명적 결함 발견 → 원인은 미리보기(docx→HTML)와 다운로드(HTML→docx)가 서로 다른 손실 변환을 타는 이중 변환 구조였음. **WYSIWYG contentEditable 편집 방식을 폐기**하고 좌표 기반 `fieldValues` 맵 + 분석/렌더링 라우트 분리 아키텍처로 전환(§4.6-1 갱신). 에이전트 B의 cellRef가 라벨 칸을 가리키는 버그를 방어 로직으로 우회, 교차검증(`isPlausibleMatch`)으로 엉뚱한 테이블 매칭 방지.
- **2026-08-23**: [중요한 자기 정정] 에이전트 B가 JSON 대신 설명문으로 응답하는 문제를 "팀원 프롬프트 이슈"로 오판했다가, 팀원 확인 요청으로 A/B 라이브 테스트를 다시 해본 결과 **개발자(Claude) 본인이 추가한 입력 텍스트 주입이 원인**이었음을 확인·정정함(QA_수정요구사항.md §4-4). 이후 같은 패턴의 에이전트 A "분류 실패" 이슈는 실제 문서 ID로 라이브 재검증까지 마친 뒤 "이력서 아닌 문서에 대한 정상 Classify 동작이었고, 우리 쪽에서 처리를 안 하고 있었을 뿐"이라고 결론 — 이번엔 추측이 아니라 검증 우선으로 접근(QA_수정요구사항.md §4-5).
- **2026-08-23**: 마감 5시간 전 체크포인트에서 기능1~7 실제 구현 상태를 코드 기준으로 재점검 — 기능3·4·7이 예상보다 덜 되어있음을 확인(§3 상단 체크포인트 참고). QA 수정 커밋을 실수로 `develop`에 직접 올렸다가(원격 push 전이라 데이터 유실 없이) `feature/qa-fixes-and-classify-fix` 브랜치로 옮기고 `develop`을 원격과 재동기화함 — 이후로는 QA/기능 수정은 항상 별도 브랜치에서 진행.
